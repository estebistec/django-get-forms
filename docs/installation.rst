============
Installation
============

At the command line::

    $ easy_install django_get_forms

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_get_forms
    $ pip install django_get_forms